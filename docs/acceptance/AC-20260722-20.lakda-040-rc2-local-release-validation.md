# AC-20260722-20: Lakda 0.4.0-rc.2 ローカルリリース検証

- 検証日時: 2026-07-22 JST
- 対象revision: `74a2a9b47cc106795320323a597dfdf5931cbead`
- branch: `main`
- 実装状態: `local_complete`
- リリース状態: `pending_external`
- 判定: ローカル実装・自動検証は完了。外部リリースは `no_go`。

## 証跡境界

fixture、mock、loopback、状態注入は補助証跡であり、実target、実機、実Qwen、認可済みsecurity探索の本証跡へ昇格しない。LakdaはQEG verdict、承認、waiverを生成しない。既存RC5 Acceptance／QEG artifactは変更していない。

## ローカル検証結果

| check | 結果 |
|---|---|
| `npm run release:validate-profile` | pass。profile SHA-256 `de90f0773d419de88525c85892ede97c15cda2dabb9c306a5bbc520541919164` |
| `npm run typecheck` | pass（最終revision） |
| `npm test -- --workers=6` | pass、198/198、exit 0（最終revision） |
| popup回帰反復 | pass、異URL／同URLの計20/20 |
| `npm run acceptance:fixture` | pass。plan 1.0、defect detection 1.0、FP 0、replay 1.0、artifact欠落0、strict JSON 1.0、unsafe 0、golden 30/30、manifest 215/215 |
| `npm run acceptance:adaptive` | pass、127/127 |
| `npm run test:contracts` / `npm run test:examples` | pass、23/23・2/2 |
| `npm run check:hate` / `npm run pack:check` | pass |
| P7 / P11 real runner | 想定どおりfail-closed。各exit 2、`pending_external`、target接続0 |

`npm run lint`はpopup修正前にpassした。最終修正後は権限レビューtimeoutで結果未取得のため、最終revisionのlint合格とは主張しない。`npm run build`も修正前にpassし、最終revisionは198件のPlaywright実行時コンパイルを通過した。最終tarball再生成は停止したため打ち切り、`pack:check`をpackage境界の証跡とする。

## manual-bb strict Gate

- 証跡: `docs/release-gate/0.4.0-rc.2/74a2a9b47cc1/manual-bb-local/`
- CLI終了code: 0
- decision: `no_go`
- P0: 2/5 pass
- P1: 1/1 pass
- case結果: 3 pass / 3 blocked

CLI正常終了とGateのGoは別である。blocked 3件は外部環境が必要なため、`pending_external`のまま保持する。

## 未完了の外部項目

- P7/P11 real acceptance
- Airtest／Poco実機
- 認可済みsecurity target
- 実Qwen
- 外部operatorのmanual-bb
- 上記証跡を入力とするQEG Gate／record

## 残余リスク

- Node v24.15.0／npm 11.12.1とpackage固定期待版に差がある。
- 同一actionから同時に複数の同URL popupが出る場合、event attributionに理論上の曖昧性が残る（P2）。
- 最終revisionのlint結果とtarball hashは未取得。

## 結論

Phase 0〜6のローカル実装、回帰検証、fail-closed確認、manual-bb入力・Gate記録は完了した。外部証跡が揃うまで `pending_external`、リリース判定は `no_go`。QEGは未実行である。