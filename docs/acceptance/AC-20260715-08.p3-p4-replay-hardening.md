---
acceptance_id: AC-20260715-08
intent_id: INT-LAKDA-001
status: reviewing
evidence_class: fixture
execution_mode: simulated
task_ids:
  - TASK.20260714-17
  - TASK.20260714-18
  - TASK.20260714-23
  - TASK.20260714-26
  - TASK.20260714-27
recorded_at: 2026-07-15
---

# AC-20260715-08: P3/P4 coverage・InputCase・strict replay hardening

## Scope

観測済み状態グラフのcoverage契約、novelty plateau、versioned InputGenerator、Web form constraint取込、raw入力値を保存しないInputCase replay、step単位strict replay照合を対象とする。

本記録はlocal HTTP fixtureとin-process Chromiumによる実装証跡である。固定16 AC corpus、Airtest/Poco実機、認可済みSecurity target、manual-bb、QEGの本証跡ではない。

## Acceptance Criteria

- discovered-model coverageが分子、分母、graph revision、open-world注記、actionごとの時系列を持つ。
- 非決定的な同一state/action outcomeでもtransition coverageが1を超えない。
- 新規state、candidate、transition、obligationの変化がnovelty plateau windowを更新する。
- InputGeneratorが同値、境界、境界直外、empty、null相当、format異常、length異常をseedから決定的に生成する。
- InputCaseがgenerator version、seed、domain ref、validity、expected oracle refを持つ。
- traceへraw入力値を保存せずvalue SHA-256だけを保存し、replay時は同じcaseを再生成してdigestを照合する。
- strict replayがstatus、pre/post fingerprint、settle、target topology、OracleResultの差分をstep直後に拒否する。

## Evidence

- [graph tests](../../tests/adaptive/graph.spec.ts): discovered-model分母、non-deterministic outcome、transition-pair、revision timeline、candidate/obligation plateau。
- [input tests](../../tests/adaptive/input.spec.ts): 7入力class、metadata、raw値非保存、value digest、dependency-valid shrinking。
- [coordinator tests](../../tests/adaptive/coordinator.spec.ts): coverage artifact timeline、pre/post divergence、exact InputCase replay、既存bridge/security/shrinking回帰。
- `npm run check`: docs、typecheck、lint、build、95/95 tests pass。
- HATE manifest回帰は既存core/adaptive integration test内でpass。固定HATE契約はcommit `3a4b655c2434109e230f8b862a9d5fe14f1c069e`、SHA-256 `9070bfeef4ecf17f6ba7eda5d7b158e3f54bf5a4577f13387f33d4835b74a263`でpass。
- Code-to-gate 1.5.0 `analyze`: pass、Critical 0、High 0、Medium 8。Mediumは `.ctg/finding-triage.json` のowner・根拠・期限（2026-08-31）と照合済み。

## Verification Result

local implementation verificationはpass。P7 statusは`pending_external`のまま維持し、mock/simulated evidenceをreal必須ACの合格へ昇格しない。

## Checklist Updates

今回の証跡で直接確認できた実装欄だけをCHECKLIST-02/03へ反映する。AC-AE-001〜016の受入Gate欄は固定corpusと外部証跡が揃うまで更新しない。
